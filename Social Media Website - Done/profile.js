const usernameSpan = document.getElementById('username');
const logoutBtn = document.getElementById('logout-btn');

// Here you can add the code to get the username of the logged in user
// and display it in the usernameSpan

logoutBtn.addEventListener('click', () => {
	// Here you can add the code to log out the user and redirect to the home page
});
